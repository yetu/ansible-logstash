---
- hosts: localhost
  remote_user: root
  vars : 
      extra_patterns_install   : true
      indexer_conf_install     : true  
  roles:
    - ansible-logstash
  post_tasks:
    - name: check if port 9456 tcp of logstash is open
      wait_for: 
        port=9456
        delay=10
